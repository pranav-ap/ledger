{
  "name": "ledger",
  "version": "1.0.0",
  "main": "server.js",
  "author": "pranav-ap <pranavap96@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^6.1.0",
    "express": "^4.16.3",
    "mongodb": "^3.1.8",
    "path": "^0.12.7"
  },
  "scripts": {
    "start": "cd client && yarn run build && node server.js",
    "start-dev": "cd client && yarn run build && nodemon server.js",
    "heroku-postbuild": "cd client && yarn --production=false && yarn run build"
  }
}